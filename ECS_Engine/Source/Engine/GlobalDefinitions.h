#pragma once

/*
Used to add a window creation function to the window manager system
The window can be added to a subwindow using a / like this: Particles/Particle System Window
*/
#define ADD_WINDOW(windowName);